# Cache + runtime state
cache/
.claude/
*.log
*.bak
*.bak.*

# Eval-harness generated artifacts. test/eval/baseline.json (when added in a
# follow-up PR) is the only file in this tree that's tracked — every other
# artifact under runs/ is per-invocation output.
test/eval/runs/

# Local config overrides
.env.local
.env

# OS noise
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Security — never commit secrets
*.pem
*.key
*.p12
*credentials*
*.token
.openai.key
.anthropic.key
.fal.key
