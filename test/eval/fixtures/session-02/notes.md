# Session 02

Exercises expanded workforce routing. The router should prefer:

- `DATABASE_ENGINEER` for migration and backfill integrity risk.
- `CFO` for billing, usage, cap, and pricing cost exposure.
- `PRE_MORTEM` for launch-failure reasoning before implementation.
- `HISTORIAN` is a useful fourth voice because the transcript names a prior pricing regression, but default router max is 3.
